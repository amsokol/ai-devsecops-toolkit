version: v2
plugins:
  # depbot: bundle buffa-codegen — member: BSR remote plugin (version family = workspace buffa)
  # depbot: hold v0.8.1 — unlock with bundle buffa-codegen (all conditions in Cargo.toml / coupled-deps.md)
  - remote: buf.build/anthropics/buffa:v0.8.1
    out: rust/api/gen/buffa
    opt:
      - file_per_package=true
      - json=true
      - views=true
